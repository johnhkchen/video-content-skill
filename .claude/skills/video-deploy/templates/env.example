# Video Deployment Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# Public Video CDN (for public videos)
# =============================================================================
# Set this to your CDN URL in production
# Leave empty or don't set for local development (will use /videos fallback)

# Generic (works with any framework)
VIDEO_CDN_URL=https://videos.yourdomain.com

# Framework-specific alternatives (use the one that matches your framework)
# Next.js (must start with NEXT_PUBLIC_ to be accessible client-side)
NEXT_PUBLIC_VIDEO_CDN_URL=https://videos.yourdomain.com

# Create React App
REACT_APP_VIDEO_CDN_URL=https://videos.yourdomain.com

# Vite
VITE_VIDEO_CDN_URL=https://videos.yourdomain.com

# =============================================================================
# Cloudflare R2 (for private videos or upload scripts)
# =============================================================================
# Get these from: Cloudflare Dashboard > R2 > Manage R2 API Tokens

R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=your-video-bucket

# =============================================================================
# AWS S3 Alternative (if not using R2)
# =============================================================================
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=your-s3-bucket

# =============================================================================
# Notes
# =============================================================================
#
# For public videos:
#   - Only VIDEO_CDN_URL (or framework-specific variant) is needed
#   - This should point to your bucket's public URL or custom domain
#
# For private videos (signed URLs):
#   - You need the full R2/S3 credentials
#   - The API route will generate temporary signed URLs
#
# Custom domain setup (recommended for R2):
#   1. Go to Cloudflare Dashboard > R2 > your-bucket > Settings
#   2. Under "Public access", connect a custom domain
#   3. Use that domain as your VIDEO_CDN_URL
